This folder is used to keep the production config for your oscr setup.

Do not check in any files here (the directory is marked as ignored).

In order to create an initial config, copy the files from the Templates dir into this folder and change them.

Templates: see oscr-gui-swing/src/main/resources/templates