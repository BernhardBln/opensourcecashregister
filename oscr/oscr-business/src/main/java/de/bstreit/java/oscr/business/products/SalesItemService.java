package de.bstreit.java.oscr.business.products;

import javax.inject.Named;

@Named
public class SalesItemService {
	//
	// @Inject
	// private HibernateTemplate hibernateTemplate;
	//
	// public AbstractSalesItem terminate(AbstractSalesItem salesItem) {
	//
	// Preconditions.checkNotNull(salesItem.getValidTo());
	// Preconditions.checkArgument(salesItem.getValidTo() != null);
	//
	// final Date now = new Date();
	// salesItem.setValidTo(now);
	//
	// return (AbstractSalesItem) hibernateTemplate.save(salesItem);
	// // TODO: notification
	// }

}
